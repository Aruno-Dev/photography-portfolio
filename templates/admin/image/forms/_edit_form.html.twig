{{ form_start(form) }}
    {{ form_widget(form) }}
        <div class="d-flex justify-content-between">
            <a class="btn btn-primary mt-2" href="{{path('admin_image')}}">Cancel</a>
            <input class="btn btn-success mt-2" type="submit" value="{{ submitButtonText }}" formnovalidate/>
        </div>
{{ form_end(form) }}