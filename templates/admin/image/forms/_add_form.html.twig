{{ form_start(form) }}
    {{ form_widget(form) }}
        <div class="d-flex justify-content-between">
            <a class="btn btn-blue mt-2" href="{{path('admin_image')}}">Cancel</a>
            <input class="btn btn-green mt-2" type="submit" value="{{ submitButtonText }}" formnovalidate/>
        </div>
{{ form_end(form) }}